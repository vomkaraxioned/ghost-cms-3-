#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@aws-sdk/client-lambda/node_modules/fast-xml-parser/src/cli/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../@aws-sdk/client-lambda/node_modules/fast-xml-parser/src/cli/cli.js" "$@"
  ret=$?
fi
exit $ret
